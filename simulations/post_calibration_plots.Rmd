---
title: EMOD Within-Host Calibration Progress
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r, out.width="90%"}
# Setup
library(tidyverse)
library(ggrepel)
library(glue)
library(gtools)
library(patchwork)
library(reticulate)

# experiment name

exp <- "250118"

coord_df <- read.csv("../simulation_inputs/calibration_coordinator.csv",header = FALSE)
# Number of training batches
training_batches <- coord_df$V2[coord_df$V1=="init_batches"]
training_size <- coord_df$V2[coord_df$V1=="init_size"]
# Batch size
batch_size <- coord_df$V2[coord_df$V1=="batch_size"]
max_eval <- coord_df$V2[coord_df$V1=="max_eval"]
dir.create(paste("output",exp,"performance",sep="/"))
score_folder<-paste("output",exp,"performance/scores",sep='/')
if(!(dir.exists(score_folder))){dir.create(score_folder)}
parameters_folder<-paste("output",exp,"performance/parameters",sep='/')
if(!(dir.exists(parameters_folder))){dir.create(parameters_folder)}
search_space_folder<-paste("output",exp,"performance/parameters/search_space",sep='/')
if(!(dir.exists(search_space_folder))){dir.create(search_space_folder)}
GP_folder<-paste("output",exp,"performance/GP",sep='/')
if(!(dir.exists(GP_folder))){dir.create(GP_folder)}
objectives_folder<-paste("output",exp,"objectives",sep='/')
if(!(dir.exists(objectives_folder))){dir.create(objectives_folder)}

# Custom color palette
my_pal <- c("gray",
            RColorBrewer::brewer.pal(8,"Paired"),
            RColorBrewer::brewer.pal(8,'Dark2'),
            RColorBrewer::brewer.pal(8,"Accent"),
            RColorBrewer::brewer.pal(8,"Set1"),
            RColorBrewer::brewer.pal(8,"Set2"),
            RColorBrewer::brewer.pal(8,"Set3"),
            RColorBrewer::brewer.pal(8,"Pastel1"),
            RColorBrewer::brewer.pal(8,"Pastel2"))
my_pal <- c(my_pal,my_pal[-1])
pal_df <-data.frame(color=my_pal)
pal_df$id <- 0:(nrow(pal_df)-1)

theme_set(theme_minimal(base_size=14))
```

### Datasets

```{r}
#### Raw scores
scores <- read.csv(paste("output",exp,'all_LL.csv',sep='/'))
# Identify parameter sets with missing or NA scores
scores %>% group_by(param_set,round) %>% summarize(missing=sum(is.na(ll))) %>% filter(missing>0) -> unscored
# Filter to only scored parameter sets
scores <- scores %>% filter(!(interaction(round,param_set) %in% interaction(unscored$round,unscored$param_set)))
# Grouped by total score (unweighted)
total_scores <- scores %>% group_by(param_set,round) %>% summarize(total_score=sum(ll*my_weight))
# Best set
best_ps = total_scores$param_set[total_scores$total_score==max(total_scores$total_score[!(total_scores$round==0 & total_scores$param_set<=6)],na.rm=T)]
best_round=total_scores$round[total_scores$total_score==max(total_scores$total_score[!(total_scores$round==0 & total_scores$param_set<=6)],na.rm=T)]
# Grouped by objective scores (unweighted)
objective_scores <- scores %>% group_by(param_set,round,metric) %>%
  summarize(objective_score=sum(ll),n_sites=length(unique(site)))

total_scores %>%
  #filter(!(round==0)) %>%
  filter(!(param_set<=6 & round==0)) %>%
  group_by(round) %>% filter(total_score==max(total_score)| (param_set=7 & round==0)) %>%
  ungroup() %>% arrange(round) %>%
  mutate(cummax=cummax(total_score)) %>% 
  filter(total_score==cummax| (param_set==7 & round==0)) -> improvements


improvements %>% group_by(round) %>% filter(param_set==max(param_set)) -> improvements

# Parameters

scores %>% ungroup() %>% select(round) -> pp

pp %>% group_by(round) %>% count() %>% select(round) ->pp

LFS <- list.dirs(path = glue('output/{exp}'),recursive = F)
LFS <- LFS[grepl("LF",LFS)]

for(i in seq(0,length(LFS)-1,1)){
  if(i==0){
    tp <- read.csv(paste(LFS[i+1],"translated_params.csv",sep='/'))
    tp %>% mutate(round=sub(".*LF_(\\d+)", "\\1", LFS[i+1])) ->  tp
  } else {
    tmp <- read.csv(paste(LFS[i+1],"translated_params.csv",sep='/'))
    tmp %>% mutate(round=sub(".*LF_(\\d+)", "\\1", LFS[i+1])) -> tmp
    tp <- rbind.data.frame(tp,tmp)
  }
}


tp %>% 
  mutate(unit_value=gsub("tensor\\(","",unit_value)) %>%
  mutate(unit_value=gsub("\\)","",unit_value)) %>%
  mutate(unit_value=as.numeric(unit_value)) %>%
  mutate(emod_value=gsub("tensor\\(","",emod_value)) %>%
  mutate(emod_value=gsub("\\)","",emod_value)) %>%
  mutate(emod_value=as.numeric(emod_value)) %>%
  mutate(emod_value=ifelse(transformation=="log",
                           log10(emod_value),
                           emod_value)) %>%
  mutate(parameter=ifelse(transformation=="log",
                          paste("Log10",parameter,sep = " "),
                          parameter))%>%
  mutate(parameter=gsub("_"," ",parameter)) %>%
  mutate(parameter=gsub("Survival Rate","\nSurvival Rate",parameter))%>%
  mutate(parameter=gsub("Inactivation","\nInactivation",parameter))%>%
  mutate(parameter=gsub("Base","",parameter)) %>%
  mutate(parameter=gsub("Falciparum","",parameter)) %>%
  mutate(parameter=gsub("Nonspecific Anti","Nonspecific Anti-\n",parameter)) -> parameters


param_levels <- unique(parameters$parameter)
param_order <- c(1,5,10,7,6,11,12,14,24,
                 4,2,9,3,
                 13,8,15,16,17,
                 18,19,20,21,22,23)
param_levels <- param_levels[param_order]

# unit values per parameter set
parameters %>% select(parameter,unit_value,param_set,round)%>%spread(parameter,unit_value) -> unit_parameters
# emod values per parameter set
parameters %>% select(parameter,emod_value,param_set,round)%>%spread(parameter,emod_value) -> emod_parameters
# parameter specifications
parameters %>% select(parameter,min,max,team_default,transformation) %>% distinct() -> parameter_key
```


### Scores

```{r}

theme_set(theme_minimal(base_size=14))

scores %>%
  mutate(weighted_ll=ll*my_weight) %>%
  mutate(bloodless=(metric=="no_blood" & weighted_ll>0)) -> scores



scores %>% ungroup() %>%
  #filter(round==0 & param_set==1) %>%
  filter(round==0 & param_set==1) %>%
  select(site,metric,ll) -> d

scores %>% ungroup() %>%
  filter(round==0 & param_set==1) %>%
  select(site,metric,ll,baseline) -> d3

# scores %>%
#   mutate(weighted_ll=ll*my_weight)%>%
#   group_by(round,param_set) %>%
#   summarize(score=sum(weighted_ll)) %>%
#   ungroup() %>%
#   group_by(round) %>% filter(score==max(score)) %>%
#   ungroup() %>% arrange(round) %>%
#   mutate(cummax=cummax(score)) %>% 
#   filter(score==cummax) -> improvements

#### Summary scores over time
scores %>%
  group_by(round,param_set) %>%
  mutate(bloodless=sum(bloodless)>0) %>% 
  ungroup() %>%
  filter(!bloodless) %>%
  group_by(round,param_set) %>%
  summarize(score=sum(weighted_ll),
            worst=max(weighted_ll),
            bloodless=sum(bloodless)) %>%
  ungroup() %>%
  #mutate(ps=ifelse(round<training_batches,param_set,ceiling(training_size/10)*10+param_set+batch_size*(round-training_batches))) %>%
  mutate(ps=round*batch_size+param_set)%>%
  ungroup() %>% 
  mutate(plotcolor=round-training_batches+2) %>% arrange(-plotcolor) %>%
  mutate(plotcolor=ifelse(plotcolor<1,1,plotcolor)) %>%
  ggplot(aes(x=ps,y=-score)) +
  geom_hline(data=.%>%filter(ps!=1) %>% filter(param_set==best_ps & round==best_round),
             aes(yintercept=-score,linetype="best"))+
  geom_point(alpha=0.33)+
  geom_point(data=.%>%filter(param_set==best_ps & round==best_round),
             aes(color="best"))+
  ylab("Total Weighted Score") + xlab("#") +
  theme_minimal(base_size=16) +
  scale_color_manual(values=my_pal[best_round+1]) +
  scale_linetype_manual(values=c(2,1)) +
  scale_y_log10() +
  labs(linetype=NULL, color=NULL)-> bp
  
  print(bp)
  ggsave(paste(score_folder,paste("scores_total.png",sep="_"),sep='/'),bp,height = 5,width=8)

  

# by round

scores %>%
  group_by(round,param_set) %>%
  mutate(bloodless=sum(bloodless)>0) %>% 
  ungroup() %>%
  filter(!bloodless) %>%
  group_by(round,param_set) %>%
  summarize(score=sum(weighted_ll),
            worst=max(weighted_ll),
            bloodless=sum(bloodless)) %>%
  group_by(round) %>% filter(score==max(score))%>%
  mutate(ps=ifelse(round<training_batches,param_set,ceiling(training_size/10)*10+param_set+batch_size*(round-training_batches))) %>%
  ungroup() %>% 
  mutate(plotcolor=round-training_batches+2) %>% arrange(-plotcolor) %>%
  mutate(plotcolor=ifelse(plotcolor<1,1,plotcolor)) %>%
  ggplot(aes(x=round,y=log10(-score))) +
  geom_hline(data=.%>%filter(ps==1),
             aes(yintercept=log10(-score),linetype="default"))+
  geom_hline(data=.%>%filter(ps!=1) %>% filter((param_set==best_ps & round==best_round)),
             aes(yintercept=log10(-score),linetype="best"))+
  geom_point(data=.%>%filter(!(round%in%improvements$round)),size=2)+
  geom_point(data=.%>%filter(round%in%improvements$round),
             aes(color=my_pal[round+1], shape="improvement"),
             size=3)+
  geom_point(data=.%>%filter(param_set==best_ps & round==best_round),
             aes(color=my_pal[round+1],shape="best"),
             size=3,fill="white")+
  ylab("Log10 Total Weighted Score") + xlab("Round #") +
  theme_minimal(base_size=16) +
  scale_color_identity()+
  scale_shape_manual(values=c(22,15))+
  scale_linetype_manual(values=c(2,1)) +
  scale_y_log10()+
  labs(linetype=NULL,shape=NULL,color=NULL)-> bp
  print(bp)
  ggsave(paste(score_folder,paste("scores_total_by_round.png",sep="_"),sep='/'),bp,height = 8,width=8)
  
  
  scores %>%
  group_by(round,param_set) %>%
  mutate(bloodless=sum(bloodless)>0) %>% 
  ungroup() %>%
  filter(!bloodless) %>%
  group_by(round,param_set) %>%
  summarize(score=sum(weighted_ll),
            bloodless=sum(bloodless)) %>%
  mutate(ps=round*batch_size+param_set)%>%
  ungroup() %>% 
  mutate(plotcolor=round-training_batches+2) %>% arrange(-plotcolor) %>%
  mutate(plotcolor=ifelse(plotcolor<1,1,plotcolor)) %>%
  ggplot(aes(x=ps,y=log10(-score))) +
  geom_hline(data=.%>%filter(ps!=1) %>% filter((param_set==best_ps & round==best_round)),
             aes(yintercept=log10(-score),linetype="best"))+
  geom_point(aes(color=my_pal[plotcolor]))+
  geom_path(data=.%>%right_join(improvements),
            aes(linetype="improvement"))+
  ylab("Log10 Total Weighted Score") + xlab("Round #") +
  theme_minimal(base_size=16) +
  scale_color_identity()+
  scale_shape_manual(values=c(22,15))+
  scale_linetype_manual(values=c(2,1)) +
  scale_y_log10()+
  scale_x_continuous(breaks = seq(0,10000,1000),
                     minor_breaks = seq(0,10000,250))+
  labs(linetype=NULL,shape=NULL,color=NULL)-> bp
  print(bp)

### All scores over time
scores %>%
  group_by(round,param_set) %>%
  mutate(bloodless=sum(bloodless)>0) %>% 
  ungroup() %>%
  filter(!bloodless) %>%
  group_by(round,param_set) %>%
  mutate(score=sum(weighted_ll),
            worst=max(weighted_ll),
            bloodless=sum(bloodless)) %>%
  mutate(ps=round*batch_size+param_set)%>%
  ungroup() %>% 
  mutate(plotcolor=round-training_batches+2) %>% arrange(-plotcolor) %>%
  mutate(plotcolor=ifelse(plotcolor<1,1,plotcolor)) %>%
  filter(metric!="no_blood") %>%
  ggplot(aes(x=ps,y=weighted_ll)) +
  facet_wrap(metric~site,scales="free_y") +
  geom_point(aes(color=my_pal[plotcolor]))+
  geom_hline(data=.%>%filter(ps!=1) %>% filter(param_set==best_ps & round==best_round),
             aes(yintercept=weighted_ll,linetype="best"))+
  
  ylab("Log10(-Weighted Score)") + xlab("#") +
  theme_minimal(base_size=16) +
  theme(legend.position=c(0.9,0),
        legend.justification = c(1,0))+
  scale_color_identity() +
  scale_linetype_manual(values=c(2,1)) +
  labs(linetype=NULL)-> bp
  
  print(bp)
  ggsave(paste(score_folder,paste("scores_objective.png",sep="_"),sep='/'),bp,height =8,width=10)

scores %>%
  group_by(round,param_set) %>%
  ungroup() %>%
  group_by(round,param_set,metric) %>%
  summarize(score=sum(weighted_ll))  %>%
  mutate(ps=round*batch_size+param_set)%>%
  ungroup() %>% 
  filter(metric!="no_blood") %>%
  ggplot(aes(x=ps,y=score)) +
  facet_wrap(~metric,scales="free_y") +
  geom_point(alpha=0.1)+
  geom_hline(data=.%>%filter(ps!=1) %>% filter(param_set==best_ps & round==best_round),
             aes(yintercept=score,linetype="best"))+
  
  ylab("-Log10(Score)") + xlab("#") +
  theme_minimal(base_size=16) +
  theme(legend.position=c(0.9,0),
        legend.justification = c(1,0))+
  scale_color_identity() +
  #scale_y_log10() +
  scale_linetype_manual(values=c(2,1)) +
  labs(linetype=NULL)-> bp

  print(bp)
  ggsave(paste(score_folder,paste("scores_objective_all.pdf",sep="_"),sep='/'),bp,height =6,width=8)
  
improvements %>% 
  left_join(scores) %>%
  ggplot(aes(x=param_set+round*batch_size,y=-weighted_ll)) +
  facet_wrap(~metric) +
  geom_line(aes(group=site,color=site)) +
  scale_y_log10() +
  geom_hline(data=.%>% filter(param_set==best_ps & round==best_round), 
             aes(yintercept=baseline*-my_weight,
                 color=site),linetype=2)
  
  
# by round only

### All scores over time
scores %>%
  group_by(round,param_set) %>%
  mutate(bloodless=sum(bloodless)>0) %>% 
  ungroup() %>%
  filter(!bloodless) %>%
  group_by(round,param_set) %>%
  mutate(score=sum(weighted_ll,na.rm = T),
            worst=min(weighted_ll,na.rm=T),
            bloodless=sum(bloodless,na.rm=T)) %>%
  group_by(round) %>% filter(score==max(score)) %>%
  mutate(ps=ifelse(round<training_batches,param_set,ceiling(training_size/10)*10+param_set+batch_size*(round-training_batches))) %>%
  ungroup() %>% 
  mutate(plotcolor=round-training_batches+2) %>% arrange(-plotcolor) %>%
  mutate(plotcolor=ifelse(plotcolor<1,1,plotcolor)) %>%
  #filter(metric!="no_blood") %>%
  ggplot(aes(x=factor(round,levels=sort(unique(scores$round))),y=-weighted_ll)) +
  facet_wrap(~metric) +
  #geom_point(aes(color=site),position = "identity")+
  geom_path(aes(color=site,group=site))+
  ylab("- Weighted Score") + xlab("Round #") +
  theme_minimal(base_size=16) +
  #scale_color_identity() +
  scale_y_log10()+
  scale_linetype_manual(values=c(2,1)) +
  labs(linetype=NULL)-> bp
  
  print(bp)
  
  ggsave(paste(score_folder,paste("scores_objective_by_round.png",sep="_"),sep='/'),bp,height =8,width=10)


scores %>%
  ggplot(aes(x=factor(round,levels=sort(unique(scores$round))),y=-weighted_ll)) + 
  geom_boxplot() +
  geom_point(data=.%>%filter((param_set==best_ps & round==best_round) | (param_set==2 & round==0)),
             aes(shape="best",color=metric))+
  scale_y_log10() +
  facet_wrap(~site) +
  guides(shape="none") +
  labs(color="best:") +
  xlab("Batch") + ylab("-Weighted Score")
    
### All points performance (sum)

bloodless=scores$param_set[scores$metric=="no_blood" & scores$ll<=-1] + batch_size*scores$round[scores$metric=="no_blood" & scores$ll<=-1]

scores %>%
  group_by(param_set,round) %>% mutate(score=sum(weighted_ll)) %>%
  group_by(round) %>% filter(score==max(score)) %>% ungroup() -> temp

temp %>%
  mutate(filltype="by_site") %>% 
  rbind.data.frame(temp%>%mutate(filltype="by_metric"))%>%
  ggplot(aes(x=interaction(filltype,param_set,round),y=-weighted_ll))+
  geom_text(aes(label=paste(ifelse(round<training_batches,paste("Init ",round),paste(round-training_batches+1))," # ",param_set," ",filltype,sep=""),
                group = weighted_ll,y=-10),
            angle=-90,check_overlap = T,size=2,hjust=-0.1) +
  geom_bar(data=.%>%filter(filltype=="by_metric"),stat="identity",position="stack",
           aes(fill=metric, group=weighted_ll),color="white",width=0.7) +
  geom_bar(data=.%>%filter(filltype=="by_site"),stat="identity",position="stack",
           aes(fill=site, group=weighted_ll),color="white",width = 0.7) +
  theme_minimal(base_size=16) +
  ylab("-Score") + xlab("") +
  labs(fill=NULL,linetype=NULL) +
  geom_vline(data=NULL, xintercept=seq(2.5,2*length(unique(temp$round)),2),
             linewidth=0.2) +
  guides(alpha="none",fill=guide_legend(ncol=7)) +
  theme(legend.position="top",
        axis.text.x=element_blank(),
        legend.box = "vertical",
        panel.grid.major.x = element_blank()) +
  scale_linetype_manual(values=c(2)) +
  scale_y_continuous(limits=c(-20000,NA)) +
  scale_fill_manual(values=my_pal)-> bp1
  
  print(bp1)

  ggsave(paste(score_folder,paste("scores_best_sets.pdf",sep="_"),sep='/'),bp1,height =12,width=16)


  # Valley Plot
  
  scores %>% 
    filter(!bloodless)%>%
    group_by(param_set,round) %>% 
    mutate(score=sum(weighted_ll),
              worst=min(weighted_ll)) %>%
    ungroup() %>%
    filter(worst==weighted_ll) %>% 
    mutate(score_rank=rank(-score)) %>%
    ggplot(aes(x=score_rank,y=worst)) +
    theme_minimal(base_size=14) +
    geom_point(aes(color=metric))  +
    facet_wrap(~site) +
    theme(legend.position = c(1,0),
          legend.justification.inside=c(1,0)) +
    labs(color=NULL) +
    #scale_y_log10()+
    scale_color_brewer(palette="Set1")+
    xlab("Rank of Total Score") + ylab("Worst Objective Score")
  
  
   scores %>% 
    filter(!bloodless)%>%
    group_by(param_set,round) %>% 
    mutate(score=sum(weighted_ll),
              worst=min(weighted_ll)) %>%
    ungroup() %>%
    filter(worst==weighted_ll) %>% 
    mutate(score_rank=rank(-score)) %>% 
    ggplot(aes(x=-score,y=-worst)) +
    theme_minimal(base_size=14) +
    geom_point(aes(color=metric))  +
    facet_wrap(~site) +
    theme(legend.position = "right",
          legend.justification.inside=c(1,0)) +
    labs(color=NULL) +
    #scale_x_continuous(breaks=seq(0,10000,2000)) +
    scale_x_log10() + 
    scale_y_log10() +
    scale_color_brewer(palette="Set1")+
    xlab("Total Score") + ylab("Worst Objective Score")
   
   scores %>%
     group_by(round,param_set)%>%mutate(score=sum(weighted_ll))%>%
     ggplot(aes(x=score,y=weighted_ll))+
     facet_wrap(~metric,scales="free_y") +
     geom_point(aes(color=log10(-weighted_ll))) +
     scale_color_fermenter(palette="Spectral")
   
   for(s in unique(scores$site)){
     scores %>%
     group_by(round,param_set)%>%mutate(score=sum(weighted_ll))%>%
     group_by(site,metric) %>%
        mutate(score_rank=rank(-score),objective_rank=rank(-weighted_ll)) %>%
     filter(site==s) %>%
       ggplot(aes(x=score_rank,y=objective_rank))+
     facet_wrap(~metric) +
     geom_point(aes(color=log10(-weighted_ll))) +
       coord_fixed()+
     ggtitle(s)+
     scale_color_fermenter(palette="Spectral") -> p
     print(p) 
   }
      

```

### Parameters

```{r}
# plot parameter search to visualize shrinking trust region / convergence / tradeoff

pk <- read.csv("parameter_key.csv")

parameters %>% select(round) -> pp

LFS <- list.dirs(path = glue('output/{exp}'),recursive = F)
LFS <- LFS[grepl("LF",LFS)]

for(i in seq(0,length(LFS)-1,1)){
  if(i==0){
    tp <- read.csv(paste(LFS[i+1],"translated_params.csv",sep='/'))
    tp %>% mutate(round=sub(".*LF_(\\d+)", "\\1", LFS[i+1])) ->  tp
  } else {
    tmp <- read.csv(paste(LFS[i+1],"translated_params.csv",sep='/'))
    tmp %>% mutate(round=sub(".*LF_(\\d+)", "\\1", LFS[i+1]))-> tmp
    tp <- rbind.data.frame(tp,tmp)
  }
}


tp %>%
  mutate(unit_value=gsub("tensor\\(","",unit_value)) %>%
  mutate(unit_value=gsub("\\)","",unit_value)) %>%
  mutate(unit_value=as.numeric(unit_value)) %>%
  mutate(emod_value=gsub("tensor\\(","",emod_value)) %>%
  mutate(emod_value=gsub("\\)","",emod_value)) %>%
  mutate(parameter=gsub("_"," ",parameter)) -> tp


# tp %>%
#   mutate(parameter=gsub("Survival Rate","\nSurvival Rate",parameter))%>%
#   mutate(parameter=gsub("Inactivation","\nInactivation",parameter))%>%
#   mutate(parameter=gsub("Base","",parameter)) %>%
#   mutate(parameter=gsub("Falciparum","",parameter)) %>%
#   mutate(parameter=gsub("Nonspecific Anti","Nonspecific Anti-\n",parameter)) -> tp


param_levels <- unique(tp$parameter)
param_order = c(1,2,3,5,9,6,10,11,7,
                 8,12,13,14,15,16,
                 4)
param_levels <- param_levels[param_order]

tp %>%
    mutate(parameter=gsub("\n","",parameter)) %>%
    mutate(parameter=gsub("  "," ",parameter)) %>%
    mutate(parameter=gsub("^\\s+|\\s+$","",parameter)) %>%
    mutate(parameter=gsub(" ","_",parameter)) -> tp

tp %>%
  mutate(round=factor(round,levels=seq(0,max(as.numeric(tp$round)),1))) -> tp

tp %>%
  left_join(pk)%>%
  filter(!(round==0 & param_set <=6)) %>% 
  ggplot(aes(x=round,shape=transformation,fill=transformation)) +
  facet_wrap(~factor(parameter,levels=unique(tp$parameter)[param_order]),
             scales="free_y") +
  geom_hline(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation=="log"),
             aes(yintercept=log10(as.numeric(team_default)),
                 linetype="default"))+
  geom_segment(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation=="log") %>%
                 group_by(round,parameter,transformation) %>% summarize(maximum=max(log10(as.numeric(emod_value))),
                                                         minimum=min(log10(as.numeric(emod_value)))),
               aes(y=minimum,yend=maximum,xend=factor(round)))+
  geom_point(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation=="log") %>%
                filter(interaction(round,param_set) %in% interaction(improvements$round,improvements$param_set)),
             aes(y=log10(as.numeric((emod_value))),
                 color=ifelse((round==best_round&param_set==best_ps),"best","improvement")))+
  geom_hline(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation!="log"),
             aes(yintercept=as.numeric(team_default),
                 linetype="default"))+
  geom_segment(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation!="log") %>%
                 group_by(round,parameter,transformation) %>% summarize(maximum=max((as.numeric(emod_value))),
                                                         minimum=min((as.numeric(emod_value)))),
               aes(y=minimum,yend=maximum,xend=factor(round,ordered = T))) +
  geom_point(data=.%>%filter(parameter%in%c("Max_Individual_Infections")),
             aes(y=as.numeric(emod_value)))+ 
  geom_point(data=.%>%filter(parameter!="InnateImmuneDistributionFlag" & transformation!="log") %>%
                filter(interaction(round,param_set) %in% interaction(improvements$round,improvements$param_set)),
             aes(y=as.numeric((emod_value)),
                 color=ifelse((round==best_round&param_set==best_ps),"best","improvement")))+
  geom_hline(data=.%>%filter(parameter=="InnateImmuneDistributionFlag"),
             aes(yintercept=as.numeric(as.factor(team_default)),
                 linetype="default"))+
  geom_point(data=.%>%filter(parameter%in%c("InnateImmuneDistributionFlag")),
             aes(y=as.numeric(as.factor(emod_value))))+ 
  geom_point(data=.%>%filter(parameter=="InnateImmuneDistributionFlag") %>%
                filter(interaction(round,param_set) %in% interaction(improvements$round,improvements$param_set)),
             aes(y=as.numeric(as.factor(emod_value)),
                 color=ifelse((round==best_round&param_set==best_ps),"best","improvement")))+
  labs(color=NULL,linetype=NULL) +
  scale_linetype_manual(values=c(2)) +
  scale_color_brewer(palette="Set1",direction = -1) +
  scale_shape_manual(values=c(21,16))+
  scale_fill_manual(values=c("white","transparent"))+
  xlab("") + ylab("EMOD parameter value range") +
  theme_minimal(base_size=12)+
   theme(legend.position="right")+
  guides(fill="none")+
  labs(color=NULL,shape="transform") + ylab("EMOD parameter value") + xlab("Batch #") -> pp
  pp

  ggsave(paste(parameters_folder,"emod_values.pdf",sep='/'),pp,height = 10,width=18)

tp %>%
  mutate(unit_value=as.numeric(unit_value))%>%
  left_join(pk)%>%
  ggplot(aes(x=factor(round))) +
  facet_wrap(~factor(parameter,levels=unique(tp$parameter)[param_order]),
             scales="free_y") +
  geom_segment(data=.%>%filter(parameter!="InnateImmuneDistributionFlag") %>%
                 group_by(round,parameter) %>% summarize(maximum=max((as.numeric(unit_value))),
                                                         minimum=min((as.numeric(unit_value)))),
               aes(y=minimum,yend=maximum,xend=factor(round))) +
  geom_point(data=.%>%filter(parameter!="InnateImmuneDistributionFlag") %>%
                filter(interaction(round,param_set) %in% interaction(improvements$round,improvements$param_set)),
             aes(y=as.numeric((unit_value)),
                 color=ifelse((round==best_round&param_set==best_ps),"best","improvement")))+
  geom_point(data=.%>%filter(parameter=="InnateImmuneDistributionFlag"),
             aes(y=as.numeric(as.factor(emod_value))))+ 
  geom_point(data=.%>%filter(parameter=="InnateImmuneDistributionFlag") %>%
                filter(interaction(round,param_set) %in% interaction(improvements$round,improvements$param_set)),
             aes(y=as.numeric(as.factor(emod_value)),
                 color=ifelse((round==best_round&param_set==best_ps),"best","improvement")))+
  labs(color=NULL,linetype=NULL) +
  scale_linetype_manual(values=c(2)) +
  scale_color_brewer(palette="Set1",direction = -1) +
  xlab("") + ylab("Unit parameter value range") +
   theme(legend.title = element_text(size=12)) ->pp
  pp
  ggsave(paste(parameters_folder,"unit_values.png",sep='/'),pp,height = 12,width=10)
  
  
tp %>% ungroup() %>%
  filter(unit_value>=0) %>%
  mutate(round=as.numeric(paste(round)))%>%
  mutate(ps=round*batch_size+param_set)%>%
  ggplot(aes(x=ps,
             y=unit_value)) +
  theme_minimal(base_size=12) +
  facet_wrap(~factor(parameter)) +
  xlab("") + ylab("Unit parameter value") +
  geom_point(aes(color=factor(round))) +
  #geom_point(data=.%>%filter(param_set==best_ps & round==best_round),
  #           aes(shape="best"),fill="white",color="black") +
  labs(color=NULL,shape=NULL,fill=NULL) +
  theme(legend.position=c(1,0),
        legend.justification=c(1,0),
        legend.box="horizontal") +
  guides(color="none") +
  scale_shape_manual(values=c(21,16)) +
  scale_color_manual(values=c(my_pal[seq(1,max(as.numeric(tp$round))+1)],"black","red")) -> pp
  pp
  ggsave(paste(parameters_folder,"unit_values_all.png",sep='/'),pp,height = 12,width=10)
  
#library(GGally)
  
# tp %>% select(parameter,unit_value,round,param_set) %>%
#     pivot_wider(names_from = parameter,values_from = unit_value) %>%
#   select(-c(param_set))%>%
#     ggpairs(mapping = aes(color=as.factor(round)))

  colnames(tp)
  
  
  tp %>%
    left_join(pk %>%rename(parameter=parameter_name))%>%
    filter(as.numeric(round)==max(as.numeric(round)))%>%
    ggplot(aes(x=unit_value)) +
    geom_abline(slope=1,linetype=2)+
    facet_wrap(~parameter) +
    stat_ecdf(lwd = 1.2,aes(group=interaction(parameter,round))) +
    scale_color_manual(values=my_pal)+ 
    labs(color=NULL) +
    theme(legend.position="right",
          legend.justification = c(1,0))

  
  tp %>% ungroup()%>%
    mutate(round=as.numeric(round))%>%
    ggplot(aes(x=as.numeric(unit_value))) +
    geom_density(aes(group=round,color=round)) +
    facet_wrap(~parameter) +
    labs(color=NULL) +
    scale_x_continuous(limits=c(0,1))
  
  tp %>% ungroup() %>%
    mutate(round=as.numeric(paste(round))) %>%
    filter(round<training_batches | round==max(round)) %>%
    mutate(label=ifelse(round<training_batches,"0",round))%>%
    ggplot(aes(x=as.numeric(unit_value))) +
    geom_dotplot(binwidth = 0.01,aes(group=label,color=label,fill=label)) +
    facet_wrap(~parameter) +
    scale_x_continuous(limits=c(0,1))
    
  tp %>% ungroup() %>%
    #filter(parameter=="Pyrogenic_Threshold") %>%
    ggplot(aes(x=round)) +
    geom_violin(aes(y=unit_value),
                draw_quantiles=c(0.5),
                scale = "width") +
    facet_wrap(~parameter)
  
  tp  %>%
  #filter(round>0)%>%
  rowwise()%>%
  mutate(parameter=gsub("_"," ",parameter),
         round=as.numeric(paste(round)))%>%
  mutate(i=round*batch_size+param_set)%>%
  mutate(unit_value=ifelse(unit_value>1 | unit_value<0,NA,unit_value))%>%
  # rowwise() %>%
  # mutate(parameter=list(unlist(strsplit(parameter, " "))))%>% 
  # mutate(parameter=split(unlist(parameter),ceiling(seq_along(parameter)/10)))->mydf
  ggplot(aes(x=i,
             y=factor(abbreviate(parameter,minlength=6),
                                                   levels=abbreviate(unique(gsub("_"," ",tp$parameter)),minlength=6)[rev(param_order)]))) +
  geom_tile(aes(fill=unit_value), color="transparent") +
  scale_fill_distiller(palette="RdGy",na.value = "yellow") +
  geom_vline(xintercept=ceiling(training_size/10)*10) +
  geom_vline(data=.%>%filter(round>training_batches),
             aes(xintercept=round*batch_size),
             linewidth=0.5) +
  xlab("") + ylab("") +
  labs(linetype=NULL)+
  #scale_linetype_manual(values=c(1,2))+
  theme(axis.text.y=element_text(size=8)) +
  scale_x_continuous(expand = c(0,0)) -> pp
  pp
  ggsave(paste(parameters_folder,"parameter_heatmap.png",sep='/'),pp,height = 10,width=16)

scores %>%
  ungroup() %>%
  mutate(i=round*batch_size+param_set)%>%
  ggplot(aes(x=i,y=interaction(site,metric))) +
  geom_tile(aes(fill=log10(-weighted_ll)),color="transparent") +
  geom_vline(xintercept=ceiling(training_size/10)*10) +
  geom_vline(xintercept=seq(ceiling(training_size/10)*10,
                            (max(as.numeric(paste(tp$round)))-training_batches+1)*batch_size + training_size,
                            batch_size),
             linewidth=0.5) +
  scale_fill_distiller(palette="RdYlGn") +
  labs(fill="-Log(score)") +
  xlab("") + ylab("") +
  theme(axis.text.y=element_text(size=8)) +
  scale_x_continuous(expand=c(0,0)) -> pp
pp
  ggsave(paste(score_folder,"score_heatmap.png",sep='/'),pp,height = 10,width=16)
  
```

```{r}
scores %>% left_join(tp %>% mutate(round=as.integer(as.numeric(round))) %>% select(param_set,round,parameter,unit_value)) %>% 
  mutate(score=ll*my_weight) %>%
  group_by(round,param_set,parameter,metric) %>%
  mutate(metric_score=sum(score)) %>%
  group_by(round,param_set,parameter) %>%
  mutate(total_score=sum(score)) %>%
  ungroup() -> spread_df

parameters <- unique(spread_df$parameter)
parameters <- parameters[!is.na(parameters)]
if(length(parameters)%%2==1){parameters <- c(parameters,parameters[1])}


for(i in seq(1,length(parameters),2)){
  j=i+1
  df_a <- spread_df %>% filter(parameter==parameters[i]) %>% select(param_set,round,metric,metric_score,parameter,unit_value)
  df_b <- spread_df %>% filter(parameter==parameters[j]) %>% select(param_set,round,metric,metric_score,parameter,unit_value)

  df_a  %>% 
    rbind.data.frame(df_b) %>%
    mutate(param_set=param_set+round*batch_size) %>% arrange(-param_set) %>%
    pivot_wider(names_from = parameter, values_from = unit_value, values_fn = mean) %>%
    arrange(round) %>%
    ggplot(aes(x=!!sym(parameters[i]),
               y=!!sym(parameters[j]))) +
    facet_wrap(~metric) +
    theme_minimal(base_size=12)+
    geom_point(aes(color=log10(-metric_score),
                   alpha=(round==max(round)))) +
    geom_point(data=.%>%filter(param_set==best_ps+best_round*batch_size),
               aes(shape="best"),color="black") +
    scale_color_distiller(palette="Spectral") +
    labs(color="Log10(-Score)",shape=NULL,alpha=NULL) +
    guides(alpha="none")+
    scale_shape_manual(values=c(21,16))+
    theme(legend.position="right")-> pp
  
  print(pp)
  ggsave(paste(paste(parameters_folder,"search_space",paste("separate",parameters[i],"x",parameters[j],sep="_"),sep="/"),".png",sep=''),pp,
         height=8,width=10)
}


for(i in seq(1,length(parameters),2)){
  j=i+1
  df_a <- spread_df %>% filter(parameter==parameters[i]) %>% select(param_set,round,total_score,parameter,unit_value)
  df_b <- spread_df %>% filter(parameter==parameters[j]) %>% select(param_set,round,total_score,parameter,unit_value)

  df_a  %>% 
    rbind.data.frame(df_b) %>%
    mutate(param_set=param_set+round*batch_size) %>% arrange(-param_set) %>%
    pivot_wider(names_from = parameter, values_from = unit_value, values_fn = mean) %>%
    arrange(round) %>%
    ggplot(aes(x=!!sym(parameters[i]),
               y=!!sym(parameters[j]))) +
    theme_minimal(base_size=16)+
    geom_point(aes(color=log10(-total_score),
                   alpha=round==max(round))) +
    geom_point(data=.%>%filter(param_set==best_ps+best_round*batch_size),
               aes(shape="best"),color="black") +
    scale_color_distiller(palette="Spectral") +
    labs(color="Log10(-Score)",shape=NULL,alpha=NULL) +
    guides(alpha="none")+
    scale_shape_manual(values=c(21,16))-> pp
  print(pp)
  ggsave(paste(paste(parameters_folder,"search_space",paste("total",parameters[i],"x",parameters[j],sep="_"),sep="/"),".png",sep=''),pp,
         height=6,width=6)
}

# for(i in seq(1,length(parameters),1)){
#   
#   for(j in seq(1,length(parameters),1)){
#     
#     if(i==j){
#       next
#     } else {
#       
#       df_a <- spread_df %>% filter(parameter==parameters[i]) %>% select(param_set,round,total_score,parameter,unit_value)
#       df_b <- spread_df %>% filter(parameter==parameters[j]) %>% select(param_set,round,total_score,parameter,unit_value)
#     
#       df_a  %>% 
#         rbind.data.frame(df_b) %>%
#         mutate(param_set=param_set+round*batch_size) %>% arrange(-param_set) %>%
#         pivot_wider(names_from = parameter, values_from = unit_value, values_fn = mean) %>%
#         arrange(round) %>%
#         ggplot(aes(x=!!sym(parameters[i]),
#                    y=!!sym(parameters[j]))) +
#         theme_minimal(base_size=16)+
#         geom_point(aes(color=log10(-total_score),
#                        alpha=round==max(round))) +
#         geom_point(data=.%>%filter(param_set==best_ps+best_round*batch_size),
#                    aes(shape="best"),color="black") +
#         geom_path(data=.%>%filter(param_set%in%(improvements$param_set+improvements$round*batch_size)),
#                   arrow = arrow(type="closed",length=unit(0.15,"in")), linetype=1)+
#         scale_color_distiller(palette="Spectral") +
#         labs(color="Log10(-Score)",shape=NULL,alpha=NULL) +
#         guides(alpha="none")+
#         scale_alpha_manual(values=c(0.33,1))+
#         scale_shape_manual(values=c(21,16)) -> pp
#       print(pp)
#       # ggsave(paste(paste(parameters_folder,"search_space",
#       #                    paste("total",parameters[i],"x",parameters[j],sep="_"),sep="/"),
#       #              ".png",sep=''),
#       #        pp,
#       #        height=6,width=6)
#     }
#   }
# }

```

```{r}
SR=read.csv("../simulation_outputs/dielmo_1990/surveyReport.csv")

SR %>% 
  mutate(age=ifelse(age>5,ifelse(age<50,trunc(age/5)*5,50),age)) %>% 
  filter(age <5 | age %in% c(5,10,15,20,50)) %>% 
  ggplot(aes(x=factor(age),y=n_strains)) + 
  geom_boxplot(aes(group=interaction(param_set,age), color=factor(param_set)))


SR %>% 
  mutate(age=ifelse(age>5,ifelse(age<50,trunc(age/5)*5,50),age)) %>% 
  filter(age <5 | age %in% c(5,10,15,20,50)) %>% 
  filter(param_set<=3) %>%
  ggplot(aes(x=n_strains,group=param_set,fill=factor(param_set)),
             color="white") + 
  geom_bar(stat="count",position=position_dodge(preserve = "single")) +
  facet_wrap(param_set~age,ncol=3,scales="free_y")

SR %>% ungroup()%>%
  mutate(age=ifelse(age>5,ifelse(age<50,trunc(age/5)*5,50),age)) %>% 
  filter(param_set<=3) %>%
  group_by(age,param_set) %>%
  summarize(parasites=mean(parasites))%>%
  ggplot(aes(x=age,y=log10(parasites))) +
  geom_line(aes(group=param_set,color=factor(param_set)))


SR %>% ungroup()%>%
  mutate(age=ifelse(age>5,ifelse(age<50,trunc(age/5)*5,50),age)) %>% 
  filter(param_set<=3) %>%
  ggplot(aes(x=age,y=log10(parasites))) +
  geom_point(aes(color=factor(param_set))) +
  geom_smooth(aes(color=factor(param_set),fill=factor(param_set)))


SR %>% 
  ungroup()%>%
  mutate(age=ifelse(age>5,ifelse(age<50,trunc(age/5)*5,50),age)) %>% 
  filter(param_set<=3) %>%
  group_by(age,param_set,bin=trunc(log10(parasites))) %>%
  summarize(n=n()) %>%
  group_by(age,param_set) %>% mutate(total=sum(n)) %>%
  ggplot(aes(x=bin,y=n/total)) +
  facet_wrap(~age) +
  geom_point(aes(color=factor(param_set))) +
  geom_path(aes(group=param_set,color=factor(param_set))) +
  theme(legend.position = c(1,0),
        legend.justification = c(1,0)) +
  labs(color="PS #") +
  xlab("Log Parasitemia Bin") +
  ylab("Fraction")

REF <- read_csv("~/b1139/within-host-calibration/reference_datasets/Cameron_age_incidence_prev.csv")

REF %>% filter(Site%in%c("dielmo_1990","ndiop_1993")) %>% select(Site,INC,INC_LAR,INC_UAR,POP) %>% mutate(incidence=INC/1000) -> REF

REF %>% 
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==4,5,INC_UAR)) %>%
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==9,10,INC_UAR)) %>%
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==14,15,INC_UAR)) %>%
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==19,20,INC_UAR)) %>%
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==39,40,INC_UAR)) %>%
  mutate(INC_UAR=ifelse(Site=="ndiop_1993" & INC_UAR==59,60,INC_UAR)) -> REF




comparison <- read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/all_LL.csv")

comparison %>% group_by(round,param_set) %>% mutate(total_score=sum(ll),bloodless=sum(metric=="no_blood" & ll!=0)>0) %>%
  ungroup() %>%
  arrange(-total_score) %>%
  group_by(site,metric) %>%
  mutate(rank=rank(-total_score)) -> best_sets

comparison %>% group_by(site,metric) %>% filter(ll==max(ll)) %>%
  filter(metric!="no_blood") %>% mutate(rank=-1,total_score=-1,bloodless=F) -> local_best

best_sets

INC <- read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_0/SO/ndiop_1993/prev_inc_by_age_annual.csv") %>% mutate(round=0)
INC <- rbind.data.frame(INC,read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_1/SO/ndiop_1993/prev_inc_by_age_annual.csv") %>% mutate(round=1))
INC <- rbind.data.frame(INC,read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_2/SO/ndiop_1993/prev_inc_by_age_annual.csv") %>% mutate(round=2))


INC %>%
  left_join(best_sets %>% filter(metric=="incidence",site=="ndiop_1993") %>% select(round,param_set,rank)) %>%
  filter((round==0 & param_set<=6) | rank==1 | interaction(round,param_set) %in% interaction(local_best$round,local_best$param_set)) %>%
  group_by(Age,round,param_set,rank) %>%
  summarize(incidence=mean(Incidence)) %>%
  ungroup() %>%
  ggplot(aes(x=Age,y=incidence)) +
  geom_path(aes(group=interaction(round,param_set),color=interaction(round,param_set))) +
  geom_path(data=.%>%filter(rank==1),color="black",aes(linetype="best"),size=1) +
  scale_x_log10(breaks=unique(INC$Age)) +
  theme(panel.grid.minor.x = element_blank()) +
  labs(linetype=NULL) +
  geom_point(data=REF%>%filter(Site=="ndiop_1993")%>%mutate(site=Site), aes(x=INC_UAR,y=incidence,size=POP),fill="white",shape=21) +
  scale_linetype_manual(values=2)

INC <- read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_0/SO/dielmo_1990/prev_inc_by_age_annual.csv") %>% mutate(round=0)
INC <- rbind.data.frame(INC,read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_1/SO/dielmo_1990/prev_inc_by_age_annual.csv") %>% mutate(round=1))
INC <- rbind.data.frame(INC,read_csv("~/b1139/within-host-calibration/simulations/output/250110_test50k/LF_2/SO/dielmo_1990/prev_inc_by_age_annual.csv") %>% mutate(round=2))


INC %>%
  left_join(best_sets %>% filter(metric=="incidence",site=="dielmo_1990") %>% select(round,param_set,rank)) %>%
  filter((round==0 & param_set<=6) | rank==1 | interaction(round,param_set) %in% interaction(local_best$round,local_best$param_set)) %>%
  group_by(Age,round,param_set,rank) %>%
  summarize(incidence=mean(Incidence)) %>%
  ungroup() %>%
  ggplot(aes(x=Age,y=incidence)) +
  geom_path(aes(group=interaction(round,param_set),color=interaction(round,param_set))) +
  geom_path(data=.%>%filter(rank==1),color="black",aes(linetype="best"),size=1) +
  scale_x_log10(breaks=unique(INC$Age)) +
  theme(panel.grid.minor.x = element_blank()) +
  labs(linetype=NULL) +
  geom_point(data=REF%>%filter(Site=="dielmo_1990")%>%mutate(site=Site), aes(x=INC_UAR,y=incidence,size=POP),fill="white",shape=21) +
  scale_linetype_manual(values=2)




```

```{r}
tp %>% mutate(round=as.numeric(paste(round)))%>% ungroup()%>%
  left_join(total_scores) %>%
  group_by(parameter) %>% 
  mutate(rank=rank(total_score)) %>% select(param_set,round,total_score,rank,unit_value) %>% 
  ungroup() %>%
  arrange(rank) %>%
  filter(rank<=5) %>%
  ggplot(aes(y=interaction("R",round,"PS",param_set,"#",rank),
             x=abbreviate(parameter))) +
  theme(axis.text.x=element_text(angle=-90)) +
  coord_fixed() +
  geom_tile(aes(fill=as.numeric(unit_value)),
            color="white") +
  labs(fill=NULL) +
  xlab("") + ylab("") +
  scale_fill_distiller(palette="Spectral")
```

